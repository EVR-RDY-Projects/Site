---
const currentYear = new Date().getFullYear();
---

<footer class="text-center" style="color:var(--muted); font-size:.95rem; margin: var(--space-lg) 0 var(--space-xl); padding-top: var(--space-lg); border-top: 1px solid rgba(229,228,216,.12);">
  <div class="container">
    <p>© {currentYear} EVR:RDY Defensive Operations Group LLC - Let us help keep the lights on!</p>
    <nav class="footer-nav">
      <a href="/privacy">Privacy</a>
      <span>·</span>
      <a href="/security">Security</a>
      <span>·</span>
      <a href="/contact">Contact</a>
      <span>·</span>
      <a href="https://github.com/EVR-RDY-Projects" target="_blank" rel="noopener noreferrer">GitHub</a>
    </nav>
  </div>
</footer>

<style>
  footer {
    background: linear-gradient(180deg, 
      rgba(0, 0, 0, 0.4) 0%, 
      rgba(0, 0, 0, 0.3) 100%);
    -webkit-backdrop-filter: blur(12px) saturate(140%);
    backdrop-filter: blur(12px) saturate(140%);
    border-radius: var(--radius-lg);
    padding: var(--space-lg);
    margin: var(--space-xl) 0;
    border: 1px solid rgba(229, 228, 216, 0.15);
    text-align: center;
    box-shadow: 
      0 10px 15px -3px rgba(0, 0, 0, 0.1),
      0 4px 6px -2px rgba(0, 0, 0, 0.05),
      inset 0 1px 0 rgba(255, 255, 255, 0.08);
    transition: all var(--transition-normal);
    position: relative;
  }

  footer::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 1px;
    background: linear-gradient(90deg, 
      transparent 0%, 
      rgba(169, 214, 138, 0.3) 50%, 
      transparent 100%);
  }

  footer:hover {
    transform: translateY(-1px);
    box-shadow: 
      0 15px 20px -3px rgba(0, 0, 0, 0.12),
      0 6px 8px -2px rgba(0, 0, 0, 0.06),
      inset 0 1px 0 rgba(255, 255, 255, 0.12);
    border-color: rgba(169, 214, 138, 0.25);
  }

  .footer-nav {
    margin-top: var(--space-sm);
    display: flex;
    justify-content: center;
    align-items: center;
    gap: var(--space-sm);
    flex-wrap: wrap;
  }

  .footer-nav a {
    color: rgba(229, 228, 216, 0.9);
    text-decoration: none;
    transition: all var(--transition-fast);
    padding: var(--space-xs) var(--space-sm);
    border-radius: var(--radius-sm);
    position: relative;
  }

  .footer-nav a::before {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(135deg, rgba(169, 214, 138, 0.1), rgba(169, 214, 138, 0.05));
    opacity: 0;
    border-radius: var(--radius-sm);
    transition: opacity var(--transition-fast);
  }

  .footer-nav a:hover::before {
    opacity: 1;
  }

  .footer-nav a:hover {
    color: var(--bright);
    text-shadow: 0 0 8px rgba(169, 214, 138, 0.4);
  }

  .footer-nav span {
    color: var(--muted);
  }
</style>
